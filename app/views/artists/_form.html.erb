<%= form_with(model: artist, local: true) do |form| %>
  <% if artist.errors.any? %>
  <div class="notification is-danger">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field is-horizontal">
    <div class="field-label is-normal">
      <%= form.label :name, :class=>"label" %>
    </div>
    <div class="field-body">
      <div class="field">
        <p class="control">
            <%= form.text_field :name, :class=>"input is-large" %>
        </p>
      </div>
    </div>
  </div>

  <div class="field is-horizontal">
    <div class="field-label is-normal">
      <%= form.label :biography, :class=>"label" %>
    </div>
    <div class="field-body">
      <div class="field">
        <p class="control">
            <%= form.text_area :biography, :class=>"textarea is-info is-medium" %>
        </p>
      </div>
    </div>
  </div>

  <div class="buttons has-addons is-right">
    <%= form.submit 'Guardar', :class=>"button is-primary is-right is-large" %>
  </div>
<% end %>
